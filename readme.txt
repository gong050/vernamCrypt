Для использования программы, необходимо указать в функции main() путь к файлу, в котором хранится текст для шифрования:

std::ifstream file(“path”); 

а также указать директорию, в которую будет сохранен файл результата шифрования:

std::ofstream output(“path”);